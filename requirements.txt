ib_async>=0.9.86
mcp>=1.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
click>=8.0.0
rich>=13.0.0
# Layer 2: regime filter — technical indicators on historical bars.
# pandas_ta 0.3.14b0 is the last stable PyPI release (the 0.4 fork has
# had packaging churn). Pinning to avoid surprises.
pandas>=2.0.0,<3.0.0
pandas_ta==0.4.71b0
numpy>=2.2.6
# Layer 7: in-house chat wrapper. Calls Anthropic API directly so we
# control the system prompt and bypass consumer-product safety overlays
# that refuse to call destructive trading tools.
anthropic>=0.40.0
# Layer 8: server-side chart rendering for get_chart / get_swing_visualization
# / etc. PNG bytes get base64-encoded into MCP ImageContent blocks so the
# chat UI (and Claude itself) can see them inline. Matplotlib only -- no
# mplfinance, since candlesticks aren't that hard to draw by hand and one
# less dep is one less surprise.
matplotlib>=3.7.0
